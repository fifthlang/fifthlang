@using RazorLight
@using Fifth.CodeGeneration;
@using fifth.metamodel.metadata.il;
@inherits BaseTemplatePage<ModuleDeclaration>

.module @Model.FileName

@foreach (var @class in Model.Classes)
{
    await IncludeAsync("classdef.cshtml", @class);
}
