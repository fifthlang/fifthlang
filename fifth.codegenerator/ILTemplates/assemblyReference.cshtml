@using RazorLight
@using Fifth.CodeGeneration;
@using fifth.metamodel.metadata.il;
@inherits BaseTemplatePage<AssemblyReference>

.assembly extern @Model.Name
{
  .publickeytoken = ( @Model.PublicKeyToken )
  .ver @Model.Version
}
